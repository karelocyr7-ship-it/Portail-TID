# Rapport — audits agents de démonstration

## Objectif

Vérifier le lancement séquentiel des agents applicatifs confirmés avec des
tâches strictement en lecture seule. ATF est exclu conformément à la demande.

## Résultat

- `tdb-agent`, `cash-recon-agent`, `revue-pdv-agent`, `gparc-agent`, `mdm-agent` et `recrutement-om-telco-agent` : runtime atteint et rapport produit.
- Les six audits sont non concluants car `bubblewrap` est indisponible dans l’environnement d’exécution Codex avant toute lecture locale.
- La file est vide et le dispatcher a traité les tâches séquentiellement.

## Sécurité

Aucun secret, fichier `.env`, dépôt applicatif distant, VM applicative ou base
de données n’a été lu ou modifié. Aucun identifiant personnel n’a été copié
vers le compte `tad-agents`.

## Action nécessaire

Installer `bubblewrap` via la procédure système validée, puis relancer les
audits en lecture seule. L’authentification de service est désormais valide.
